import React from 'react';
import '../styles/Lobby.css';

const Lobby = () => {
    return (
        <div className="Lobby">
            <div className="Lobby-room-list"></div>
            <button>Create Room</button>
        </div>
    );
};

export default Lobby;
